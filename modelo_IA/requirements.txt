# ==============================================================================
# Vigilia — modelo_IA (pipeline S1..S10)
# torch/torchvision: el Dockerfile los instala primero desde el indice CPU de
# PyTorch para no descargar ~2 GB de CUDA. Para GPU, ver el Dockerfile.
# ==============================================================================

numpy
scipy                    # S4 Tracking: algoritmo hungaro (linear_sum_assignment)
opencv-python-headless   # captura RTSP, preprocesado, render de clips y visualizador
ultralytics              # S3 YOLO y S5 Pose
torch
torchvision
Flask                    # S10 Visualizador (stream MJPEG)
python-dotenv            # carga de modelo_IA/.env en ejecucion local
pyyaml                   # S7: reglas en YAML (opcional)
